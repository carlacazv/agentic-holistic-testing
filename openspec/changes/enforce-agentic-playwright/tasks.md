## 1. Contract and Runtime

- [x] 1.1 Add the versioned architecture, expanded file kinds, cleanup metadata, pinned configuration, and CI command contract to the v1 schema.
- [x] 1.2 Add a pinned TypeScript parser and AST helpers for imports, calls, class declarations, object construction, strings, and properties.
- [x] 1.3 Enforce canonical/profile-aware paths, central fixture composition, dependency injection, tags, annotations, test structure, factories, cleanup, locator ownership, and forbidden TypeScript patterns.
- [x] 1.4 Expand the implementation checksum to cover architecture, candidate mapping, configuration, CI, paths, kinds, and sources.

## 2. Generation Guidance and Benchmarks

- [x] 2.1 Rewrite the implementation skill instructions around the executable architecture contract and repository preflight.
- [x] 2.2 Replace the local fixture suite with the canonical layered architecture and keep it executable.
- [x] 2.3 Load the corrected Planning Poker suite as a golden validator fixture and trigger its real E2E workflow on contract changes.
- [x] 2.4 Add negative regressions for monolithic specs, direct imports, manual object/context construction, missing metadata, missing factories/cleanup, bad paths, and stale evidence.

## 3. Verification

- [ ] 3.1 Run schema, Node, fixture Playwright, performance, adapter, OpenSpec, and contamination validation.
- [ ] 3.2 Run the Planning Poker external benchmark through three zero-retry repetitions.
- [ ] 3.3 Confirm all pull-request checks are green and document the protected-main enforcement path.
