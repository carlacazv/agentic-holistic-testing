## 1. Implementation Contract

- [ ] 1.1 Implement candidate, source, locator, assertion, config, CI, and verification validation; verify unapproved, sleep-based, and inaccessible implementations fail.
- [ ] 1.2 Render implementation manifest, findings, and verification artifacts through RunStore and verify completion gates.

## 2. Fixture Execution

- [ ] 2.1 Add the exact Playwright dev dependency via the public registry and build the isolated web/API fixture app, configuration, fixtures, and TypeScript tests.
- [ ] 2.2 Execute API and browser tests three times with zero retries and verify configured HTML, JSON, JUnit, trace, screenshot, and video outputs.

## 3. Skill Delivery

- [ ] 3.1 Add/register instructions and update docs; verify a clean Codex adapter contains four checksum-matched skills.
- [ ] 3.2 Run full validation twice, archive the specification, and push verified commits.
