## 1. Suitability semantics

- [x] 1.1 State in the automation-strategy instructions that a level is suitable only when it gives confidence equivalent to the levels above it, that the recommendation is the lowest level marked suitable, and that a risk visible only higher up is declared by withholding the lower level's suitability; verify the existing rejection of a row recommended above its flags still holds.

## 2. Component-object signal

- [x] 2.1 Rewrite the implement-playwright component-object guidance so the decision comes from repetition of a widget across the approved browser specs, and state that strategy rows carry no component signal because a component-suitable case never becomes a Playwright candidate; verify `npm run validate:adapter` succeeds for both providers.
