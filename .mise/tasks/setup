#!/bin/bash
#MISE description="First-run onboarding: install dependencies and build both provider skill layouts"
#MISE depends=["node:install", "build:all"]

set -e

echo "Setup complete. Built layouts are under dist/codex and dist/claude."
echo "To install into another project, run there: npx github:carlacazv/agentic-holistic-testing install <codex|claude>"
